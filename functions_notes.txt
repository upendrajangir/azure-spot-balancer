node_pool.py
    1. list_node_pools -> list all nodepools information in a cluster
    2. get_node_pool -> get a nodepool information in a cluster
    3. scale_node_pool -> scale a nodepool in a cluster manualy e.g. incerease nodecount to 3
    4. enable_node_pool_autoscaling
    5. disable_node_pool_autoscaling
    6. update_node_pool_autoscaling -> update nodepool autoscaling configuration
    7. scale_node_pool_manual -> scale a nodepool in a cluster manualy e.g. incerease nodecount to 3
    8. increment_node_pool -> increment a nodepool in a cluster
    9. decrement_node_pool -> decrement a nodepool in a cluster

node.py
    1. list_nodes -> list all nodes in a cluster
    2. list_nodes_in_nodepool -> list all nodes in a nodepool
    3. get_node -> get a node information in a cluster
    4. delete_node -> delete a node in a cluster (k8s way)
    5. delete_node_in_nodepool -> delete a node in a nodepool (azure way)
    6. get_node_status -> get a node status in a cluster
    7. cordon_node -> cordon a node in a cluster
    8. uncordon_node -> uncordon a node in a cluster
    9. drain_node -> drain a node in a cluster ##No Need of this
    10. 

workload.py
    1. list_workloads -> list all workloads in a cluster
    2. list_workloads_in_nodepool -> list all workloads in a nodepool
    3. list_workloads_in_node -> list all workloads in a node
    4. list_workloads_in_namespace -> list all workloads in a namespace
    5. get_workload -> get a workload information in a cluster
    6. update_workload_pdb -> update a workload pod disruption budget in a cluster
    7. get_resource_quota -> get a resource quota of a workload in a cluster
    8. get_resource_quota_in_node -> get a resource quota of a workload in a nodepool
    9. evict_workload -> evict a workload in a cluster
    10. evict_workload_in_nodepool -> evict a workload in a nodepool
    11. evict_workload_in_node -> evict a workload in a node
    12. evict_workload_in_namespace -> evict a workload in a namespace

utils.py
1. get_
1. calculate_resource_quota -> calculate resource quota of workload on a node
2. 